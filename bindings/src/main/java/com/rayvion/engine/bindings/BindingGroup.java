package com.rayvion.engine.bindings;

/**
 * Represents a logical group of related key bindings (e.g., "Movement", "Combat").
 *
 * @param name the unique name of the binding group
 */
public record BindingGroup(String name) {
}
