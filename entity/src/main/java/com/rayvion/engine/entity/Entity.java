package com.rayvion.engine.entity;

/**
 * Represents a lightweight game object in the engine.
 * Entities are identified by a unique ID and serve as containers for various systems.
 *
 * @param id the unique identifier of the entity
 */
public record Entity(long id) { }

